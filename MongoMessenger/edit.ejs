<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mini-Chat | Edit Chat</title>
    <link rel="stylesheet" href="/style.css">
</head>

<body>
    <div class="container">
        <h1>Edit Message from **<%= ch.from %>** to **<%= ch.to %>** 📝</h1>

        <form method="POST" action="/chats/<%= ch._id %>/edit">

            <div class="form-group">
                <label for="sender">From:</label>
                <input type="text" id="sender" value="<%= ch.from %>" disabled>
            </div>

            <div class="form-group">
                <label for="reciver">To:</label>
                <input type="text" id="reciver" value="<%= ch.to %>" disabled>
            </div>

            <div class="form-group">
                <label for="msg">Message:</label>
                <textarea id="msg" name="msg" required><%= ch.msg %></textarea>
            </div>

            <div class="form-actions">
                <button type="submit" class="btn btn-primary">Update Chat ✅</button>
                <a href="/chats" class="btn btn-secondary">Cancel</a>
            </div>
        </form>
    </div>
</body>

</html>